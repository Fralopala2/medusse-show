# Features / Funcionalidades

## ES — Funcionalidades
- Arquitectura por capas con componentes desacoplados
- Datos en tiempo real (WebSocket) y dashboards (Grafana)
- Pipeline MQTT → Telegraf → InfluxDB
- Múltiples clientes: Web (Next.js) + App (Flutter)
- Enfoque demo pública: **solo lectura** (cuando esté online)

## EN — Features
- Layered architecture with decoupled components
- Realtime data (WebSocket) + dashboards (Grafana)
- MQTT → Telegraf → InfluxDB pipeline
- Multiple clients: Web (Next.js) + App (Flutter)
- Public demo focus: **read-only** (once online)