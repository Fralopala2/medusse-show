# Medusse API REST (copia)

Copia del README de `api/` para servir desde la web.

## Endpoints principales

- `GET /health`
- `GET /api/locations`
- `GET /api/data/:location/:sensor`
- `GET /api/latest/:location`

Para instrucciones completas, ver el `api/README.md` en el repositorio.
